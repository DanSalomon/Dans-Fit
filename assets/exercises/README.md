# Exercise Images

Each PNG here is referenced by `src/constants/exerciseCatalog.ts` via `require()`.

The current files are 64×64 transparent placeholder PNGs. Replace them with real exercise illustrations (recommended ~256×256, transparent background) keeping the **exact same filenames** so the catalog continues to resolve them.
